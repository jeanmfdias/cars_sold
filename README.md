# Cars Sold

To run

`composer install`

`cp .env.example .env`

`php artisan key:generate`

Set `.env` file with database configurations

`php artisan migrate`

`php artisan serve`

Requirements

* PHP 8.2.6
* Postgresql
